#!/bin/bash
#
# Example of what we expect:
#   .Key ->
#   .Value -> consul-8500.service.consul

{{range ls "published"}}
    {{ with $d := .Value | parseJSON }}export BACKEND_{{ $d.port }}={{ $d.service }}.service.consul{{end}}
{{end}}
